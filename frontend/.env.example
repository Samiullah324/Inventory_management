# Backend API base URL (Vite exposes env vars prefixed with VITE_)
# For local dev with Vite proxy, /api is sufficient.
# For production or direct backend access: http://localhost:8000/api
VITE_API_URL=/api
